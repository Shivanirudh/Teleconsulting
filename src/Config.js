const config = {
  port: 3000,
  apiUrl: `http://localhost:8081`
};

export default config;


// axios.get('https://localhost:8081/api/v1/auth/generate/patient/otp', {
//     httpsAgent: new https.Agent({ 
//         rejectUnauthorized: false 
//     })
// })

// 'ngrok-skip-browser-warning': 'true'